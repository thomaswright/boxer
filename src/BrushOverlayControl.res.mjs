// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Array from "rescript/lib/es6/Belt_Array.js";
import * as JsxRuntime from "react/jsx-runtime";

let options = [
  [
    "none",
    "None"
  ],
  [
    "overlay",
    "Shadow"
  ],
  [
    "color",
    "Color"
  ]
];

function buttonClass(isActive) {
  return "flex-1 text-xs font-medium px-1 py-1  first:rounded-l last:rounded-r border transition-colors " + (
    isActive ? "bg-[color:var(--plain-900)] text-[color:var(--plain-white)] border-[color:var(--plain-900)]" : "bg-[color:var(--plain-white)] text-[color:var(--plain-800)] border-[color:var(--plain-200)] hover:border-[color:var(--plain-400)]"
  );
}

function BrushOverlayControl(props) {
  let setOverlayMode = props.setOverlayMode;
  let overlayMode = props.overlayMode;
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsx("div", {
        children: "Brush Overlay",
        className: "font-medium"
      }),
      JsxRuntime.jsx("div", {
        children: Belt_Array.map(options, param => {
          let label = param[1];
          let mode = param[0];
          return JsxRuntime.jsx("button", {
            children: label,
            className: buttonClass(overlayMode === mode),
            type: "button",
            onClick: param => setOverlayMode(param => mode)
          }, label);
        }),
        className: "grid grid-cols-3"
      })
    ],
    className: "p-2 flex flex-col gap-2 w-full"
  });
}

let make = BrushOverlayControl;

export {
  options,
  buttonClass,
  make,
}
/* react/jsx-runtime Not a pure module */
